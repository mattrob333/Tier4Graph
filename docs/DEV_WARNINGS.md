# Things AI Must Never Do

1. Never modify schema without checking ONTOLOGY.md
2. Never introduce global state outside dependency injection
3. Never change relationship names in the graph
4. Never introduce new services without approval
5. Never break the folder structure
6. Never create undocumented endpoints
7. Never use print() for logging (use loguru/logging)
8. Never assume frontend behavior — check the frontend code
9. Never commit migrations or schema changes without explanation
